# https://taskfile.dev

version: '3'

vars:
  GREETING: Hello, World!

tasks:
  default:
    cmds:
      - echo "{{.GREETING}}"
    silent: true
  test:
    cmds:
      - uv run pytest
      - cd streamlit_json_tip/frontend && npm test -- --coverage --ci --watchAll=false
  